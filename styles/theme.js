import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  fonts: {
    heading:
      "InterVariable, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif",
  },
});
export default theme;
